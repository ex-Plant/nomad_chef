# --- Postgres (local Docker) ---
POSTGRES_DB=chef
POSTGRES_USER=chef
POSTGRES_PASSWORD=chef
DOCKER_CONTAINER=chef-cms

# --- Payload (required to boot) ---
# Local dev: postgres://chef:chef@localhost:5434/chef
# Prod: Neon connection string
DB_POSTGRES_URL=postgres://chef:chef@localhost:5434/chef
PAYLOAD_SECRET=change-me

# --- Media (Vercel Blob) ---
BLOB_READ_WRITE_TOKEN=

# --- SMTP (Nodemailer) ---
# Used by the contact form server action and (once uncommented) the
# nodemailer adapter in payload.config.ts.
EMAIL_HOST=
EMAIL_USER=
EMAIL_PASS=
# Inbox that receives contact-form submissions. Falls back to EMAIL_USER if unset.
EMAIL_TO=

# --- Seed scripts (local-only; only needed when running `pnpm seed:admin`) ---
# SEED_ADMIN_EMAIL=
# SEED_ADMIN_PASSWORD=
